## Summary

-

## SEO/content checklist

- [ ] I used a short-lived branch; this PR does not directly update `release`.
- [ ] Public claims are supported by the project source material.
- [ ] `package.json` has a valid `X.Y.Z` version with a higher final segment than the PR base.
- [ ] The final version commit is `chore(version): bump root version to vX.Y.Z`.
- [ ] I ran `pnpm lint` and `pnpm build`, or explained why either could not run.

## Verification

-
